apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: sleepr
  region: eu-north-1

nodeGroups:
  - name: ng-1
    instanceType: t3.micro
    desiredCapacity: 5
    maxSize: 5
    minSize: 5

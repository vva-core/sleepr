apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: gateway
  name: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  strategy: {}
  template:
    metadata:
      labels:
        app: gateway
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
        - image: 805102730154.dkr.ecr.eu-north-1.amazonaws.com/sleepr/gateway:latest
          name: gateway
          env:
            - name: PORT
              value: '3003'
            - name: AUTH_GRPC_URL
              value: 'auth-grpc:50051'
            - name: RESERVATIONS_GRPC_URL
              value: 'reservations-grpc:50052'
            - name: PAYMENTS_GRPC_URL
              value: 'payments-grpc:50053'
          ports:
            - containerPort: 3003

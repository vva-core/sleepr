apiVersion: v1
kind: Service
metadata:
  labels:
    app: payments
  name: payments-grpc
spec:
  ports:
    - name: payments-grpc
      port: 50053
      protocol: TCP
      targetPort: 50053
  selector:
    app: payments
  type: ClusterIP

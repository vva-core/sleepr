apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth
  name: auth-grpc
spec:
  ports:
    - name: auth-grpc
      port: 50051
      protocol: TCP
      targetPort: 50051
  selector:
    app: auth
  type: ClusterIP
